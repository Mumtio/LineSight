"""L1 ACQUISITION - frame sources: a folder, a video file, or a live camera.

All three implement ``base.FrameSource``; ``base.open_source`` builds the one
named by ``config.acquisition.source_type`` (ADR-006).
"""
