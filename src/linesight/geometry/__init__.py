"""L2 GEOMETRY - where a frame sits on the roll, and how big a pixel is.

``aruco.py`` is the layer proper: it turns a frame into a ``FrameGeometry``
(absolute position, mm/px, fabric ROI). ``calibration.py`` declares the bench
constants ArUco cannot supply.
"""
