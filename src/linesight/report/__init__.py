"""L8 PRODUCT - the documents a roll leaves behind.

``pdf.py`` renders the filed report (and the plain-text version the CLI
prints); ``defect_map.py`` draws the positional map, the score strip, and the
per-event evidence crops the report embeds.
"""
